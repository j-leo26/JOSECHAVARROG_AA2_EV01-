@model IEnumerable<MVCCRUD.Models.Aprendix>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreAprendiz)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ApellidoAprendiz)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreAprendiz)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ApellidoAprendiz)
            </td>
            <td>
                <a asp-action="Edit" class="btn btn-warning" asp-route-id="@item.IDaprendiz">Edit</a> |
                    <a asp-action="Details" class="btn btn-primary" asp-route-id="@item.IDaprendiz">Details</a> |
                    <a asp-action="Delete" class="btn btn-danger" asp-route-id="@item.IDaprendiz">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
